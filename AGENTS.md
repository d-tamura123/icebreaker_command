# AGENTS.md

このファイルは、AIコーディングエージェント(Claude Code、Codex、Cursor等)がこのリポジトリで
作業する際に読み込む、プロジェクト共通の指示ファイルです。人間向けの`README.md`とは役割が異なり、
「コードから推測できない情報」「毎回説明するのが面倒な前提知識」を中心に書いています。

## プロジェクト概要

`icebreaker_command`(流氷溶かし隊)は、DXLib + 自作フレームワーク(dxe/tnl、`common/`以下)を用いた
Windows向け3Dゲームです。プレイヤーは船を操作し、流氷を攻撃して航路を確保します。

- 言語: C++
- グラフィックス/入出力: DXLib
- 独自ラッパー/ユーティリティ: `common/`以下の`dxe`(DXLibラップ層)・`tnl`(数学・汎用ユーティリティ)
- ゲーム本体のコード: `000_GameDevelopment/program`以下
- ビルド: `DxeSolution.sln`(Visual Studio)

<!-- TODO: 具体的なビルド手順(構成/プラットフォーム指定、必要なSDK等)をここに追記する -->

## リポジトリ構成の要点

- `program/object/` : ゲーム内オブジェクト(船、弾、氷山等)
- `program/effect/` : VFX(スプライトアニメーション、ビルボード描画)関連
- `program/weapon/` : 攻撃(弾の発射・管理)関連
- `program/scene/` : シーン管理
- `common/` : DXLibラップ層(`dxe`)・数学/汎用ユーティリティ(`tnl`)。**担当外の成果物のため変更不可**

## コーディング規約

コメントの書き方・変数命名の方針は `docs/coding_style_guide.md` に詳細をまとめています。
コードを新規作成・修正する際は、**必ずこのファイルを読んでから作業すること**。特に以下は最重要:

- 数式・数学的ロジックは省略せず書き下す
- 関数・処理ブロックは「一文要約→詳細」の順で説明する
- 変数名は「何を求めたいか」が名前だけで分かるようにする(`dx`/`dy`等の省略名は避ける)
- **設計上の選択肢が複数ありうる判断(演出・アーキテクチャ等)は、実装前に選択肢を提示し、
  合意を得てから進めること**(独断で実装を進めない。詳細は`docs/coding_style_guide.md`の12節)

## 変更してはいけないもの

- `common/`以下(DXLibラップ層・汎用ライブラリ)は担当外の成果物のため変更不可。
  修正が必要な場合は、変更せずその旨を報告すること。
